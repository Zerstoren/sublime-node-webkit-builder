{
    // Path for compiled project file.
    "save_to": ".",

    // Autopack ( if true) will create .nw archive upon every run. This will slow down window opening
    "autopack": false,


    /*  Linux                                                                       */
    // "nw_command": "/usr/bin/node-webkit",
    // "nw_flags": [ "--developer" ],

    /*  OS X                                                                        */
    "nw_command": "open",
    "nw_flags": [ "-a", "/Applications/node-webkit.app", "--args", "--developer" ],

    /*  Windows (NOT TESTED)                                                        */
    // "nw_command": "node-webkit.exe",
    // "nw_flags": [ "-a", "/Applications/node-webkit.app", "--args", "--developer" ],

    // Files/dirs to ignore (not a regexp)
    "ignore" :
    [
        "/.git/"
    ]
}